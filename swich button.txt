<div class="auth-btn">
  <a href="Login.html" class="auth-link">Login</a>
  <a href="Register.html" class="auth-link active">Register</a>
</div>




.auth-btn {
  display: inline-flex;
  align-items: center;
  background: rgba(12, 182, 63, 0.15);
  border: 1px solid rgba(12, 182, 63, 0.4);
  border-radius: 999px;
  padding: 4px;
  gap: 4px;
  backdrop-filter: blur(8px);
}

/* Links */
.auth-link {
  padding: 8px 22px;
  border-radius: 999px;
  text-decoration: none;
  font-weight: 600;
  font-size: 14px;
  color: #0a5c2a;
  transition: all 0.35s ease;
  position: relative;
  z-index: 1;
}

/* Active Button */
.auth-link.active {
  background: linear-gradient(135deg, #0cb63f, #069c34);
  color: #fff;
  box-shadow: 0 8px 20px rgba(12, 182, 63, 0.35);
}

/* Hover Effect */
.auth-link:not(.active):hover {
  color: #0cb63f;
}

/* Micro interaction */
.auth-link:active {
  transform: scale(0.96);
}
